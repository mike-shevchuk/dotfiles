diff --git a/apps/tui/src/index.tsx b/apps/tui/src/index.tsx
index 124429e..95d801e 100644
--- a/apps/tui/src/index.tsx
+++ b/apps/tui/src/index.tsx
@@ -1393,10 +1393,18 @@ function AgentListItem(props: AgentListItemProps) {
             </text>
           </box>
 
-          {/* Row 2: thread name */}
-          <Show when={props.agent.threadName}>
+          {/* Row 2: window name + thread name */}
+          <Show when={props.agent.windowName || props.agent.threadName}>
             <text truncate>
-              <span style={{ fg: isUnseen() ? color() : P().overlay0 }}>{props.agent.threadName}</span>
+              <Show when={props.agent.windowName}>
+                <span style={{ fg: isUnseen() ? color() : P().subtext0 }}>{props.agent.windowName}</span>
+              </Show>
+              <Show when={props.agent.windowName && props.agent.threadName}>
+                <span style={{ fg: P().overlay0 }}>{" · "}</span>
+              </Show>
+              <Show when={props.agent.threadName}>
+                <span style={{ fg: isUnseen() ? color() : P().overlay0 }}>{props.agent.threadName}</span>
+              </Show>
             </text>
           </Show>
         </box>
diff --git a/integrations/tmux-plugin/scripts/focus.sh b/integrations/tmux-plugin/scripts/focus.sh
old mode 100644
new mode 100755
diff --git a/integrations/tmux-plugin/scripts/server-common.sh b/integrations/tmux-plugin/scripts/server-common.sh
old mode 100644
new mode 100755
diff --git a/packages/runtime/src/contracts/agent.ts b/packages/runtime/src/contracts/agent.ts
index 0d55297..4f288de 100644
--- a/packages/runtime/src/contracts/agent.ts
+++ b/packages/runtime/src/contracts/agent.ts
@@ -11,6 +11,8 @@ export interface AgentEvent {
   unseen?: boolean;
   /** Set by pane scanner — the tmux pane ID where this agent was detected */
   paneId?: string;
+  /** Set by pane scanner — the tmux window name where this agent was detected */
+  windowName?: string;
 }
 
 export const TERMINAL_STATUSES = new Set<AgentStatus>(["done", "error", "interrupted", "stale"]);
diff --git a/packages/runtime/src/server/index.ts b/packages/runtime/src/server/index.ts
index fde002a..e8ed35f 100644
--- a/packages/runtime/src/server/index.ts
+++ b/packages/runtime/src/server/index.ts
@@ -413,6 +413,7 @@ export function startServer(mux: MuxProvider, extraProviders?: MuxProvider[], wa
         threadId: presence.threadId,
         threadName: presence.threadName,
         paneId: presence.paneId,
+        windowName: presence.windowName,
       });
     }
 
@@ -1019,6 +1020,8 @@ export function startServer(mux: MuxProvider, extraProviders?: MuxProvider[], wa
     if (!targetPaneId) return;
 
     log("focus-agent-pane", "focusing", { sessionName, agentName, paneId: targetPaneId });
+    // Switch to the window containing the pane first, then focus the pane
+    shell(["tmux", "select-window", "-t", targetPaneId]);
     shell(["tmux", "select-pane", "-t", targetPaneId]);
 
     const existing = pendingHighlightResets.get(targetPaneId);
@@ -1051,6 +1054,7 @@ export function startServer(mux: MuxProvider, extraProviders?: MuxProvider[], wa
     agent: string;
     session: string;
     paneId: string;
+    windowName?: string;
     threadId?: string;
     threadName?: string;
     status?: import("../contracts/agent").AgentStatus;
@@ -1231,7 +1235,7 @@ export function startServer(mux: MuxProvider, extraProviders?: MuxProvider[], wa
 
     const raw = shell([
       "tmux", "list-panes", "-a",
-      "-F", "#{session_name}|#{pane_id}|#{pane_pid}|#{pane_current_command}|#{pane_title}",
+      "-F", "#{session_name}|#{pane_id}|#{pane_pid}|#{pane_current_command}|#{pane_title}|#{window_name}",
     ]);
     if (!raw) return result;
 
@@ -1240,12 +1244,14 @@ export function startServer(mux: MuxProvider, extraProviders?: MuxProvider[], wa
       const idx2 = line.indexOf("|", idx1 + 1);
       const idx3 = line.indexOf("|", idx2 + 1);
       const idx4 = line.indexOf("|", idx3 + 1);
+      const idx5 = line.indexOf("|", idx4 + 1);
       return {
         session: line.slice(0, idx1),
         id: line.slice(idx1 + 1, idx2),
         pid: parseInt(line.slice(idx2 + 1, idx3), 10),
         cmd: line.slice(idx3 + 1, idx4),
-        title: line.slice(idx4 + 1),
+        title: line.slice(idx4 + 1, idx5 >= 0 ? idx5 : undefined),
+        windowName: idx5 >= 0 ? line.slice(idx5 + 1) : undefined,
       };
     });
 
@@ -1296,6 +1302,7 @@ export function startServer(mux: MuxProvider, extraProviders?: MuxProvider[], wa
           agent: agentName,
           session: pane.session,
           paneId: pane.id,
+          windowName: pane.windowName,
           threadId,
           threadName,
           status,
